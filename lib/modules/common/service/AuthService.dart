/// TODO(ibrahim-mubarak): Need better way of registering Services
/// We should have mixins which provides ApiClient to the service natively
class AuthService {
  static Future<void> login() async {}
}
